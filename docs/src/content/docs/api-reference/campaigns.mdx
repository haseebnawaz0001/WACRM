---
title: Campaigns
description: Send bulk messages to multiple contacts
---

import { Aside } from '@astrojs/starlight/components';

## Overview

Campaigns allow you to send bulk WhatsApp messages to multiple contacts using approved templates. The API handles rate limiting and delivery tracking automatically.

## List Campaigns

Retrieve all campaigns.

```bash
GET /api/campaigns
```

### Query Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `page` | integer | Page number (default: 1) |
| `limit` | integer | Page size, 1–100. Default `50`. |
| `status` | string | Filter by status |
| `whatsapp_account` | string | Filter by WhatsApp account **name** |
| `search` | string | Case-insensitive match on the campaign name |
| `from` / `to` | string | `YYYY-MM-DD` range filter on `created_at` (`to` is inclusive of the whole day) |

Ordered by `created_at DESC`.

### Response

```json
{
  "status": "success",
  "data": {
    "campaigns": [
      {
        "id": "uuid",
        "name": "Holiday Promotion",
        "whatsapp_account": "Main Business",
        "template_id": "uuid",
        "template_name": "holiday_offer",
        "status": "completed",
        "total_recipients": 1000,
        "sent_count": 1000,
        "delivered_count": 950,
        "read_count": 500,
        "failed_count": 50,
        "scheduled_at": "2024-01-01T10:00:00Z",
        "started_at": "2024-01-01T10:00:05Z",
        "completed_at": "2024-01-01T10:30:00Z",
        "created_by_name": "Jane Admin",
        "updated_by_name": "Jane Admin",
        "created_at": "2024-01-01T09:00:00Z",
        "updated_at": "2024-01-01T10:30:00Z"
      }
    ],
    "total": 25,
    "page": 1,
    "limit": 50
  }
}
```

<Aside type="note">
  The account is referenced by **name** via `whatsapp_account` — there is no `account_id` field.
  Campaigns that use a media header also return `header_media_id`, `header_media_filename` and
  `header_media_mime_type`.
</Aside>

## Get Campaign

Retrieve a single campaign with detailed progress.

```bash
GET /api/campaigns/{id}
```

### Response

```json
{
  "status": "success",
  "data": {
    "id": "uuid",
    "name": "Holiday Promotion",
    "template_id": "uuid",
    "template_name": "holiday_offer",
    "whatsapp_account": "Main Business",
    "status": "processing",
    "total_recipients": 1000,
    "sent_count": 450,
    "delivered_count": 400,
    "read_count": 100,
    "failed_count": 10,
    "scheduled_at": null,
    "started_at": "2024-01-01T10:00:05Z",
    "created_at": "2024-01-01T09:00:00Z",
    "updated_at": "2024-01-01T10:05:00Z"
  }
}
```

## Create Campaign

Create a new campaign. Per-recipient template values are supplied when you import recipients, not
on the campaign itself — there is no `variable_mapping` field.

```bash
POST /api/campaigns
```

### Request Body

```json
{
  "name": "New Year Sale",
  "whatsapp_account": "Main Business",
  "template_id": "uuid",
  "header_media_id": "",
  "scheduled_at": "2024-01-01T00:00:00Z"
}
```

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `name` | string | Yes | Campaign name |
| `whatsapp_account` | string | Yes | **Name** of the sending WhatsApp account |
| `template_id` | uuid | Yes | Template to send |
| `header_media_id` | string | No | Meta media ID for a media-header template — upload it first with `POST /api/campaigns/{id}/media` |
| `scheduled_at` | string | No | ISO-8601 time to start the campaign |

### Response

```json
{
  "status": "success",
  "data": {
    "id": "uuid",
    "name": "New Year Sale",
    "status": "draft",
    "created_at": "2024-01-01T00:00:00Z"
  }
}
```

## Update Campaign

Update a draft campaign.

```bash
PUT /api/campaigns/{id}
```

<Aside type="note">
  Only draft campaigns can be updated. Started or completed campaigns cannot be modified.
</Aside>

## Delete Campaign

Delete a campaign.

```bash
DELETE /api/campaigns/{id}
```

## Import Recipients

Import recipients from a CSV or JSON file.

```bash
POST /api/campaigns/{id}/recipients/import
```

### Request Body (JSON)

```json
{
  "recipients": [
    {
      "phone_number": "+1234567890",
      "recipient_name": "John Doe",
      "template_params": { "customer_name": "John", "coupon": "SAVE20" },
      "header_params": { "season": "Summer" }
    },
    {
      "phone_number": "+0987654321",
      "recipient_name": "Jane Smith",
      "template_params": { "customer_name": "Jane", "coupon": "SAVE15" },
      "header_params": { "season": "Winter" }
    }
  ]
}
```

`header_params` is only needed for templates with a TEXT header variable
(Meta allows at most one). It's kept separate from `template_params` so a
positional header `{{1}}` doesn't collide with body `{{1}}`. Omit the field
entirely for templates without a header variable.

### Response

```json
{
  "status": "success",
  "data": {
    "message": "Recipients added successfully",
    "added_count": 2,
    "total_recipients": 1002
  }
}
```

`added_count` is the number of recipients in this request; `total_recipients` is the campaign's new
total.

## Get Recipients

Get a campaign's recipients with their per-recipient delivery status.

```bash
GET /api/campaigns/{id}/recipients
```

### Query Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `page` | integer | Page number (default `1`) |
| `limit` | integer | Page size, 1–200. Default `50` |
| `status` | string | `pending`, `sent`, `delivered`, `read` or `failed` |

The list is read to answer "who did not get it", which is what the `status` filter is for. It is paged because a campaign can hold hundreds of thousands of recipients.

### Response

`total` is the number matching the filter, not the size of the page.

```json
{
  "status": "success",
  "data": {
    "recipients": [
      {
        "id": "uuid",
        "campaign_id": "uuid",
        "phone_number": "1234567890",
        "recipient_name": "John Doe",
        "status": "delivered",
        "template_params": { "customer_name": "John", "coupon": "SAVE20" },
        "header_params": { "season": "Summer" },
        "whatsapp_message_id": "wamid.xxx",
        "error_message": "",
        "sent_at": "2024-01-01T10:00:10Z",
        "delivered_at": "2024-01-01T10:00:15Z",
        "read_at": "2024-01-01T10:05:00Z"
      }
    ],
    "total": 1000,
    "page": 1,
    "limit": 50
  }
}
```

| Field | Type | Description |
|-------|------|-------------|
| `phone_number` | string | Recipient phone number |
| `recipient_name` | string | Recipient display name |
| `status` | string | `pending`, `sent`, `delivered`, `read`, or `failed` |
| `template_params` | object | Body parameter values used for this recipient |
| `header_params` | object | TEXT-header parameter values (if any) |
| `whatsapp_message_id` | string | Meta message ID once sent |
| `error_message` | string | Failure reason when `status` is `failed` |
| `sent_at` / `delivered_at` / `read_at` | string | Delivery timestamps (null until reached) |

<Aside type="note">
  When phone-number masking is enabled for the organization, `phone_number` and
  any phone-like `recipient_name` values are masked in the response.
</Aside>

## Delete Recipient

Remove a single recipient from a campaign. Only allowed while the campaign is in
`draft` status.

```bash
DELETE /api/campaigns/{id}/recipients/{recipientId}
```

### Response

```json
{
  "status": "success",
  "data": {
    "message": "Recipient deleted successfully"
  }
}
```

## Upload Campaign Media

Upload the media asset for a campaign whose template has a media header (image,
video, or document). Allowed only for `draft` campaigns whose template has a
non-TEXT header. The file is uploaded to WhatsApp and stored locally for preview.

```bash
POST /api/campaigns/{id}/media
```

Send the request as `multipart/form-data` with a `file` field (max 16 MB).

```bash
curl -X POST "http://your-server:8080/api/campaigns/{id}/media" \
  -H "X-API-Key: whm_your_api_key" \
  -F "file=@banner.jpg"
```

### Response

```json
{
  "status": "success",
  "data": {
    "media_id": "meta-media-id",
    "filename": "banner.jpg",
    "mime_type": "image/jpeg",
    "local_path": "campaigns/uuid.jpg",
    "message": "Media uploaded successfully"
  }
}
```

## Get Campaign Media

Serve the previously uploaded campaign media file (used for previews). Returns
the raw media bytes, not a JSON envelope.

```bash
GET /api/campaigns/{id}/media
```

## Campaign Actions

### Start Campaign

Begin sending messages.

```bash
POST /api/campaigns/{id}/start
```

### Pause Campaign

Pause a running campaign.

```bash
POST /api/campaigns/{id}/pause
```

### Cancel Campaign

Cancel a campaign (cannot be resumed).

```bash
POST /api/campaigns/{id}/cancel
```

### Retry Failed

Re-queue every recipient whose delivery `failed`. Allowed only on `completed`,
`paused`, or `failed` campaigns. Failed recipients are reset to `pending`, the
campaign moves back to `processing`, and the messages are re-enqueued.

```bash
POST /api/campaigns/{id}/retry-failed
```

#### Response

```json
{
  "status": "success",
  "data": {
    "message": "Retrying failed messages",
    "retry_count": 12,
    "status": "processing"
  }
}
```

### Get Progress

Fetch live progress for a campaign. This returns the same payload as
[Get Campaign](#get-campaign) (status plus sent/delivered/read/failed counts) and
is convenient for polling while a campaign is sending.

```bash
GET /api/campaigns/{id}/progress
```

## Audience

A campaign can be aimed at a **segment** instead of an uploaded recipient list.

```bash
PUT  /api/campaigns/{id}/audience
POST /api/campaigns/{id}/audience/preview
```

```json
{
  "audience_type": "segment",
  "segment_id": "…"
}
```

The audience can only be set while the campaign is a draft or scheduled. Changing the aim after a send has started would mean the recipient list no longer matches what went out.

### Materialisation

Recipients are written when the campaign **starts**, not when the audience is set — from the manual button and from the scheduler alike. A campaign scheduled last week therefore goes to whoever matches today.

The filter is snapshotted at the same moment, so "who did this go to, and why" survives later edits to the segment.

### Preview

```bash
POST /api/campaigns/{id}/audience/preview
```

Returns the count, **who is being left out and why**, and ten rendered samples:

```json
{
  "status": "success",
  "data": {
    "count": 9410,
    "excluded": {
      "opted_out": 2980,
      "invalid_phone": 12,
      "duplicate": 8,
      "recently_mailed": 140
    },
    "sample": [
      { "contact_id": "…", "name": "Ayesha", "phone": "+923001234567", "preview": "Hi Ayesha, we have news." }
    ]
  }
}
```

"12,400 contacts" and "12,400 minus 3,000 who opted out" are different decisions, which is why the exclusions are itemised rather than netted off.

The samples are **rendered**, not the raw template: approving `Hi {{1}}` tells nobody what the customer will read.

| Exclusion | Meaning |
|---|---|
| `opted_out` | Marketing opt-out. Applies to MARKETING templates only |
| `invalid_phone` | The number does not normalise to anything sendable |
| `duplicate` | Two contacts share a number; messaging the same person twice in one campaign is the visible failure |
| `recently_mailed` | The organization's marketing frequency cap |

### Marketing frequency cap

An organization can set `marketing_frequency_cap_hours` in **Settings → General**. A contact who has received a marketing campaign within that window is excluded from the next one, across campaigns.

It is **off by default**. Opt-out is the only protection a contact has otherwise, and it is all-or-nothing: somebody sent four marketing campaigns in a week has no way to ask for fewer without asking for none. It is not switched on for anyone automatically, because a number chosen on an organization's behalf would silently drop recipients from campaigns they had already approved.

The cap applies to MARKETING templates only. A delivery notification or a one-time code is not what anybody means by "too many messages".

It applies to **segment-targeted** campaigns. An uploaded recipient list is a list of numbers rather than of contacts, and a number that matches nothing in the CRM has no history to cap against.

## Parameter mapping

Instead of a value per recipient in a CSV, a campaign can map each of the template's parameters to a contact field:

```json
{
  "param_mappings": {
    "customer_name": { "source": "contact.name", "fallback": "there" },
    "policy": { "source": "contact.fields.policy_number", "fallback": "—" },
    "branch": { "source": "static", "value": "Lahore" }
  }
}
```

| Source | Resolves to |
|---|---|
| `contact.name` | The contact's name |
| `contact.phone_number` | Their number |
| `contact.fields.<key>` | A custom field |
| `static` | The literal in `value`, the same for everyone |

The sources are a closed list rather than an arbitrary path: a campaign renders for thousands of contacts at once, and a typo that silently produced empty values would be found by the recipients.

Mappings are keyed by the **template's own parameter names**, so a template that gains a parameter does not shift every value along by one.

`fallback` is what is sent when the source resolves to nothing. Meta rejects a send with an empty parameter, so without a fallback a contact missing a company name fails instead of sending — the fallback turns "this one recipient has a gap" into a message that still goes out.

Validation reports **which** parameters a mapping does not cover, rather than refusing with "invalid", so the campaign screen can name them.

## Campaign Status

| Status | Description |
|--------|-------------|
| `draft` | Campaign created, not yet started |
| `scheduled` | Campaign scheduled for future sending |
| `queued` | Recipients have been pushed onto the Redis job queue |
| `processing` | Workers are actively sending messages |
| `paused` | Campaign is paused |
| `completed` | All messages have been processed |
| `cancelled` | Campaign was cancelled |
| `failed` | The campaign could not be processed |

## Throughput

Sends are dispatched through a Redis Streams job queue and consumed by worker processes. Throughput
is governed by how many workers you run (`-workers=N` on `server` or `worker`), not by a built-in
per-second throttle — WA CRM does **not** shape traffic to your Meta messaging tier. If you
exceed your tier, Meta rejects the sends and the affected recipients are marked `failed`; use
[Retry Failed](#retry-failed) once your limits allow.

<Aside type="tip">
  Start with smaller campaigns to warm up your account and improve your messaging tier.
</Aside>
